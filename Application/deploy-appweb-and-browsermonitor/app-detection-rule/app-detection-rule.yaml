config:
- app-detect-rule: app-detect-rule.json

app-detect-rule:
- name: "{{.Env.Tag_app}}_{{.Env.DomainName}}_{{.Env.Tag_env}}"
- domainname: "{{.Env.DomainName}}"
- applicationId: "/application-web/app.id"
