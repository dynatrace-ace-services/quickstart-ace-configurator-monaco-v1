config:
- browsermonitor: browser-monitor.json

browsermonitor:
- name: "{{.Env.Dt_app}}.{{.Env.Dt_env}} - {{.Env.DomainName}}"
- url: "http://{{.Env.DomainName}}"
- domainname: "{{.Env.DomainName}}"
- enable: "{{.Env.EnableSynthetic}}"
- applicationId: "/application-web/app.id"

