config:
- ondemand: ondemand-mw.json
ondemand:
- name: "OnDemand-{{.Env.Tag-app}}_{{.Env.Tag-env}}"
- app: "{{.Env.Tag-app}}"
- env: "{{.Env.Tag-env}}"
- start: "{{.Env.Start}}"
- stop: "{{.Env.Stop}}"
